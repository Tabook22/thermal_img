server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /thermal { return 301 /thermal/; }
    location /thermal/ {
        try_files $uri $uri/ /thermal/index.html;
    }
    location / { return 404; }
}
